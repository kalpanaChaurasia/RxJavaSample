package com.rxjavasample;

public enum Operators {
    //Create observable
    Create, Defer, From, Interval, Just, Range, Repeat, Timer,
    //Transforming
    Buffer, Map, FlatMap, SwitchMap, ConcatMap, GropuBy, Scan,
    //Filtering
    Debounce, ElementAt, Distinct, Filter, IgnoreElement, Sample, Skip, SkipLast, Take, TakeLast


}
